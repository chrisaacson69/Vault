
# OAuth credentials - never commit
client_secret*.json
request.token
*.zip
youtubeuploader.exe

# Game ROMs and emulator save states — keep out of the repo
*.nes
*.mss

# External tools live in ~/source/repos/ and are pointed at from project
# READMEs / tool-pointer files — see projects/game-annotation/nobunaga/tools/EXTERNAL.md

# Obsidian plugins — installed per-clone via Community Plugins
.obsidian/plugins/
.obsidian/community-plugins.json

# Node dependencies and build output (eleventy)
node_modules/
_site/
